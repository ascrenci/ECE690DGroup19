import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, iPhone!")
                .font(.largeTitle)

            Text("Built on GitHub Actions")
                .padding()
        }
    }
}
