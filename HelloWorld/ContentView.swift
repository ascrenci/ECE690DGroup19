import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("Hello, iPhone!")
                .font(.largeTitle)

            Text("Running from build!")
                .padding()
        }
    }
}
